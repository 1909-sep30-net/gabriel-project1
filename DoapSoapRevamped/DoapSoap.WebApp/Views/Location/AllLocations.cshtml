@model IEnumerable<DoapSoap.BusinessLogic.Models.Location>

@{
    ViewData["Title"] = "AllLocations";
}

<h1>AllLocations</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.ActionLink("Order History", "OrderHistory", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Inventory", "Inventory", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
