Things that still need working on:

- Logging (preferrably through a non static member)
- Fixing a bug with the location inventory being added to rather than depleted (mightve fixed already)
- Make some more custom css stuff